# Pinned to the versions actually installed and used to produce every result
# in this repository (Python 3.13.2, see README.md for the exact python/tf
# version printout). Generated from `pip freeze`, trimmed to the packages this
# repo's code directly imports -- see pip_freeze_full.txt for the complete
# resolved environment including transitive dependencies.

numpy==2.5.1
pandas==3.0.3
matplotlib==3.11.0
scikit-learn==1.9.0
tensorflow==2.21.0
# Pinned to 4.13.x, not the newest 5.0.x: opencv-python==5.0.0.93 does not
# expose cv2.CascadeClassifier in its Python bindings (verified directly,
# see opencv-experiments/README.md and mini-projects/face-or-object-detector/).
opencv-python==4.13.0.92
seaborn==0.13.2
jupyter==1.1.1
pillow==12.3.0

# Added for final-project/src/qat.py (quantization-aware training). tfmot's
# default quantize_model() needs the legacy tf.keras compat layer to import
# under TF 2.21/Keras 3 (see qat.py's TF_USE_LEGACY_KERAS=1 at the top).
# Installing tensorflow-model-optimization downgraded absl-py from 2.5.0 to
# 1.4.0 (its own pin is absl-py~=1.2); verified the rest of the repo's
# scripts still run correctly under absl-py 1.4.0.
tensorflow-model-optimization==0.8.1
tf-keras==2.21.0
